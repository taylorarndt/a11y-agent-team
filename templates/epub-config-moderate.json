{
  "$schema": "https://raw.githubusercontent.com/taylorarndt/a11y-agent-team/main/schemas/epub-scan-config.schema.json",
  "version": "1.0",
  "description": "Moderate ePub profile — errors and warnings only. Good default for most organizations.",
  "epub": {
    "enabled": true,
    "disabledRules": [],
    "severityFilter": ["error", "warning"],
    "maxFileSize": 104857600
  }
}
