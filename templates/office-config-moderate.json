{
  "$schema": "https://raw.githubusercontent.com/taylorarndt/a11y-agent-team/main/schemas/office-scan-config.schema.json",
  "version": "1.0",
  "description": "Moderate profile — errors and warnings only. Good default for most organizations.",
  "docx": {
    "enabled": true,
    "disabledRules": [],
    "severityFilter": ["error", "warning"]
  },
  "xlsx": {
    "enabled": true,
    "disabledRules": [],
    "severityFilter": ["error", "warning"]
  },
  "pptx": {
    "enabled": true,
    "disabledRules": [],
    "severityFilter": ["error", "warning"]
  }
}
