{
  "$schema": "https://raw.githubusercontent.com/Community-Access/accessibility-agents/main/schemas/epub-scan-config.schema.json",
  "version": "1.0",
  "description": "Minimal ePub profile — errors only. Good for quick triage of large ePub libraries.",
  "epub": {
    "enabled": true,
    "disabledRules": [],
    "severityFilter": ["error"],
    "maxFileSize": 104857600
  }
}
